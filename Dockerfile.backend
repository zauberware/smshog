FROM node:18-alpine

WORKDIR /app

COPY backend/package*.json ./

# Install dependencies and TypeScript globally
RUN npm install && npm install -g typescript

COPY backend/ ./

# Build the application
RUN npm run build || echo "Build command failed, check if build script exists in package.json"

EXPOSE 3000

CMD ["npm", "start"]
