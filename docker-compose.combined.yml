services:
  smshog:
    build:
      context: .
      dockerfile: Dockerfile.combined
    ports:
      - "${PORT:-8080}:80"
    environment:
      - NODE_ENV=production
      - SMSHOG_PERSIST=true
      - PORT=8080
    volumes:
      - ./data:/app/backend/data:rw
      
